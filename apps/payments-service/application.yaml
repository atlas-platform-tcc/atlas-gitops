# Generated by Atlas. Argo CD Application for service payments-service.
# Combines the shared base chart (atlas-templates) with the per-service values
# (atlas-gitops, via the $values multi-source reference). The values pin the base
# image built from the service's initial commit, so merging this brings up the base
# version once the image is built and the namespace (atlas-infra) exists.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: payments-service
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: atlas
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    - repoURL: https://github.com/atlas-platform-tcc/atlas-templates.git
      targetRevision: main
      path: charts/base
      helm:
        valueFiles:
          - $values/apps/payments-service/values.yaml
    - repoURL: https://github.com/atlas-platform-tcc/atlas-gitops.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: payments-service
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
